#!/bin/bash

. /www/tr069/common.sh

if [ "$ACTION"x = "set"x ];then
    uci set cwmp.cwmp.pass="$VALUE"
    uci commit cwmp
else
    echo 'admin'
    _ret=`uci get cwmp.cwmp.pass 2>/dev/null`
    echo $_ret
fi

exit 0
