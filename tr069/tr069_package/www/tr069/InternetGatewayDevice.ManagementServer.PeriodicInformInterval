#!/bin/bash

. /www/tr069/common.sh

if [ "$ACTION"x = "set"x ];then
    uci set cwmp.cwmp.periodic_interval="$VALUE"
    uci commit cwmp
else
    echo '900'
    exit 0
    _ret=`uci get cwmp.cwmp.periodic_interval 2>/dev/null`
    echo $_ret
fi

exit 0
